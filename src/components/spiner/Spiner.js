import spiner from './spinner.gif'

const Spiner = () => {
    return <img src={spiner} 
                alt="loading" 
                style={{display: 'block', margin: 'auto'}}/>
}

export default Spiner